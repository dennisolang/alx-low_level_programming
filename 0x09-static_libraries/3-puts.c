#include "main.h"
void _puts(char *str)
#include<stdio.h>
/**
 * main - Entry point
 *_puts - output of characters and strings
 * Return: Always 0 (Success)
 */
int main(void)
{
_puts("\"Programming is proving to be fun\"\n\t- Dennis Olang");
return (0);
}
